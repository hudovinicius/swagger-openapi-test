openapi: "3.2.0"
info: {title: Rejected querystring parameter, version: 1.0.0}
paths:
  /manual/items:
    get:
      tags: [manual-negative]
      parameters:
        - name: rawQuery
          in: querystring
          content:
            application/x-www-form-urlencoded:
              schema: {type: object}
      responses:
        "200": {description: Not representable}
