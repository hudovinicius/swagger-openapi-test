openapi: "3.2.0"
info: {title: Rejected boolean schema, version: 1.0.0}
paths:
  /manual/items:
    get:
      tags: [manual-negative]
      responses:
        "200":
          description: Not representable
          content:
            application/json:
              schema: true
