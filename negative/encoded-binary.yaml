openapi: "3.2.0"
info: {title: Rejected encoded binary, version: 1.0.0}
paths:
  /manual/items/{id}:
    post:
      tags: [manual-negative]
      requestBody:
        content:
          application/json:
            schema:
              type: string
              contentEncoding: base64
              contentMediaType: application/pdf
      responses:
        "204": {description: Not representable}
